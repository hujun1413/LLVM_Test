#include <stdio.h>
#include <stdlib.h>

#include <unistd.h>

#include "sample.h"
#include "sample1.h"

int
main (int argc, char ** argv)
{
  printf ("%d\n", compute_sample (5));
  printf ("%d\n", compute_sample1 (6));
  exit (0);
}

